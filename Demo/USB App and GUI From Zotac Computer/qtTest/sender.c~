#include <stdio.h>
#include <string.h>

int cnt;
int main()
{
	while (1) {
        cnt++;

printf("%d\n",cnt%3 );
/*
        if (cnt%3 == 0)
        {
          //  printf("0\n");
              printf("%d\n",cnt%3 );
        }
        else if (cnt%3 == 1)
        {
           // printf("1\n");
	      printf("%d\n",cnt%3 );
        }
        else if (cnt%3 == 2)
        {
           // printf("2\n");
	      printf("%d\n",cnt%3 );

        }
*/

		fflush(stdout);
        sleep(3);
	}
	
	return 0;
}
